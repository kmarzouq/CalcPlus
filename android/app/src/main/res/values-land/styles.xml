<?xml version="1.0" encoding="utf-8"?>
<!--
  Landscape keypad: rows share the available height by weight and buttons fill
  their row, so nothing clips no matter how short the window is. (Portrait
  keeps wrap-content rows inside a ScrollView — see values/styles.xml.)
-->
<resources>
    <style name="CalcRow">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="CalcKey">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:background">@drawable/key_background</item>
        <item name="android:textColor">@color/key_text</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:insetLeft">0dp</item>
        <item name="android:insetRight">0dp</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="CalcKey.Sci">
        <item name="android:textColor">@color/key_text_op</item>
        <item name="android:textSize">15sp</item>
        <item name="android:minHeight">0dp</item>
    </style>
</resources>
